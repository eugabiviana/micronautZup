datasources:
  default:
    url: jdbc:mysql://localhost:3306/carroGRPC?createDatabaseIfNotExist=true
    driverClassName: com.mysql.cj.jdbc.Driver

 # Habilita log do contexto transactional

logger:
  levels:
    io.micronaut.transactional: DEBUG